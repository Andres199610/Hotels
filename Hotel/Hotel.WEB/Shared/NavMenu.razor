<div class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="">Hoteles</a>

        
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-nav"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="flex-wrap">
        
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="/countries">
                <span class="oi oi-globe" aria-hidden="true"></span> Paises
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="/rooms">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Habitacion
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="/bookings">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Reserva
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="/companies">
                <span class="oi oi-list-rich" aria-hidden="true"></span> Hotel
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
